"use client";
import {useMemo,useState} from "react";
import {calculateAtlasScore} from "../../lib/scoring";
export default function ScoringPage(){const [income,setIncome]=useState(50000);const [debt,setDebt]=useState(8000);const [months,setMonths]=useState(24);const [history,setHistory]=useState(80);const [previous,setPrevious]=useState(2);const [requested,setRequested]=useState(8000);const r=useMemo(()=>calculateAtlasScore({monthlyIncome:income,monthlyDebt:debt,employmentMonths:months,paymentHistory:history,previousLoans:previous,requestedAmount:requested}),[income,debt,months,history,previous,requested]);return <main className="min-h-screen bg-slate-50"><header className="border-b bg-white"><div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-5"><div className="text-2xl font-bold">Créditos<span className="text-sky-600">Punta</span></div><a href="/" className="text-sm font-semibold">← Dashboard</a></div></header><div className="mx-auto max-w-6xl px-6 py-8"><h1 className="text-3xl font-bold">Score Atlas</h1><p className="mt-1 text-slate-500">Motor de evaluación preliminar. No constituye una decisión crediticia.</p><div className="mt-8 grid gap-6 lg:grid-cols-2"><section className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="font-bold">Datos</h2><div className="mt-5 grid gap-4 sm:grid-cols-2">{[["Ingreso mensual",income,setIncome],["Deuda mensual",debt,setDebt],["Antigüedad (meses)",months,setMonths],["Historial (-100 a 100)",history,setHistory],["Préstamos previos",previous,setPrevious],["Monto solicitado",requested,setRequested]].map(([label,value,setter]:any)=><label key={label} className="text-sm font-medium">{label}<input type="number" value={value} onChange={e=>setter(+e.target.value)} className="mt-2 w-full rounded-lg border px-3 py-2"/></label>)}</div></section><section className="rounded-2xl border bg-white p-6 shadow-sm"><div className="flex items-end justify-between"><div><div className="text-sm text-slate-500">Score</div><div className="text-6xl font-bold">{r.score}</div></div><span className="rounded-full bg-slate-100 px-3 py-1 text-sm font-semibold">{r.category}</span></div><div className="mt-6 grid grid-cols-2 gap-4"><div className="rounded-xl bg-slate-50 p-4"><div className="text-sm text-slate-500">Endeudamiento</div><div className="mt-1 text-xl font-bold">{r.debtRatio}%</div></div><div className="rounded-xl bg-slate-50 p-4"><div className="text-sm text-slate-500">Máximo sugerido</div><div className="mt-1 text-xl font-bold">${r.maxSuggestedAmount.toLocaleString("es-UY")}</div></div></div><h3 className="mt-6 font-semibold">Factores</h3><ul className="mt-3 space-y-2 text-sm">{r.reasons.map(x=><li key={x}>• {x}</li>)}</ul></section></div></div></main>}
