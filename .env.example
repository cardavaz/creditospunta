# PostgreSQL connection string for local/staging development
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/creditospunta?schema=public"

# Secreto para firmar la cookie de sesión (JWT HS256). Generar uno propio, por ejemplo:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET="reemplazar-con-un-secreto-aleatorio"
